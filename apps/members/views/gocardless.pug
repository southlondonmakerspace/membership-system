extends /src/views/base.pug

block prepend title
	- title = 'GoCardless'
	- page = 'gocardless'

block contents
	.row
		.col-md-3
			include partials/sidebar.pug
		.col-md-9
			+page_header( title )
			form( method="post" ).form-horizontal
				+csrf
				+input( 'text', 'Mandate ID', 'mandate_id', { value: member.gocardless.mandate_id } )
				+input( 'text', 'Subscription ID', 'subscription_id', { value: member.gocardless.subscription_id } )
				+input( 'number', 'Minimum', 'minimum', { before: '£', after: '.00', value: member.gocardless.minimum, placeholder:Options( 'gocardless-minimum' ) } )
				+form_button( 'Update' )
